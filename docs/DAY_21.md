# Day 21: Embeddings Concept

Today we added a local embedding function in `app/vector_search.py`.

Instead of using a paid embedding model, the project creates a simple token/skill-weighted vector so we can learn the retrieval flow first.
